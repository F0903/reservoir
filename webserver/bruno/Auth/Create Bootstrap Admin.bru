meta {
  name: Create Bootstrap Admin
  type: http
  seq: 2
}

post {
  url: {{url}}/api/auth/bootstrap
  body: json
  auth: none
}

body:json {
  {
    "username": "{{admin_username}}",
    "password": "{{admin_password}}"
  }
}

settings {
  encodeUrl: true
}
