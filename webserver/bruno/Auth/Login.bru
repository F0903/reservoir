meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "placeholder"
  }
}

settings {
  encodeUrl: true
}
